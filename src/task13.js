const FirstWord = 'Программирование'
const dash = '-'
const message = 'это искусство создавать "мозаику кода"'
const dot = '.'
const result = (`${FirstWord} ${dash} ${message}.`);
console.log(result);